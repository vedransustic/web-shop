export type DataType = {
    id: number
    img: string
    title: string
    desc: string
    bg: string
}

export type CategoryType = {
    id: number
    categoryName: string
    image: string
}

export type ProductType = {
    id: number,
    image: string
}